[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
addopts = -v --tb=short
markers =
    unit: Testes unitários rápidos
    integration: Testes de integração (APIs, Banco de Dados, Webhooks)
    environment: Testes de integridade de ambiente e governança
